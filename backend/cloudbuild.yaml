steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/systemy-backend:latest', '-f', 'backend/Dockerfile', 'backend/']

images:
  - 'gcr.io/$PROJECT_ID/systemy-backend:latest'

options:
  logging: CLOUD_LOGGING_ONLY
