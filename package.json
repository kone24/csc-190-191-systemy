{
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "postgres": "^3.4.8"
  },
  "scripts": {
    "test": "cd backend && npx jest --verbose && cd ../frontend && npx jest --verbose",
    "test:backend": "cd backend && npx jest --verbose",
    "test:frontend": "cd frontend && npx jest --verbose",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}